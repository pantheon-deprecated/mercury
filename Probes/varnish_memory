#!/bin/bash
#set varnish memory usage

RAM=`grep MemTotal /proc/meminfo | sed 's/[^0-9]*//g'`

if [[ -a /etc/varnish/memory.sh ]]; then
   /etc/varnish/memory.sh
elif (($RAM>=1000000)); then
    echo "256"
else
    echo "128"
fi
